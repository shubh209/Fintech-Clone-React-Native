# Incremental Feature Structure Migration

We will migrate obvious files into the new feature/domain structure first and leave uncertain files until they have a clear owner. Crypto market screens, API client code, chart helpers, shared theme, shared formatting, shared metrics, and shared UI are the first migration targets.

This avoids a risky all-at-once folder rewrite while still moving the codebase toward the agreed architecture.
