# Descriptive Grouped Type Files

Feature type files may group related types together when those types describe the same product area. Use descriptive names like `CryptoMarketTypes.ts` instead of vague names like `types.ts`.

We will not split every tiny type into its own file unless a type becomes important enough to deserve its own named contract.
