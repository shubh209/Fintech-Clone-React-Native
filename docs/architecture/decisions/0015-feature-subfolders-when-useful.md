# Feature Subfolders When Useful

Feature folders may use technical subfolders such as `screens`, `api`, `components`, `chart`, and `types` when the feature has enough code to justify them. Tiny features can stay flatter until a real grouping appears.

Folders should explain the shape of the feature, not exist as empty architecture placeholders.
